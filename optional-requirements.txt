# This is a list of optional Python packages for use with Abacus.  They are mostly
# used for analysis routines.
# The file is in pip format and can be installed with:
# $ pip install -r optional-requirements.txt

# There is also a requirements.txt file in this directory.  To install
# both sets of requirements at the same time, use:
# $ pip install -r requirements.txt -r optional-requirements.txt

astropy
Corrfunc
h5py
tabulate
pyfftw
pynbody
asdf
toml
globus_sdk
fair_research_login
braceexpand
pytest
